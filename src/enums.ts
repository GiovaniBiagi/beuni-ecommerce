export enum SortOptions {
  "featured" = "Recomendados",
  "price-asc" = "Menor para maior preço",
  "price-desc" = "Maior para menor preço",
}

export enum FilterOptions {
  "category" = "category",
  "sortBy" = "sortBy",
  "min" = "min",
  "max" = "max",
  "q" = "q",
}
